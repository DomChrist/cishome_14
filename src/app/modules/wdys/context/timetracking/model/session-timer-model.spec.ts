import { SessionTimerModel } from './session-timer-model';

describe('SessionTimerModel', () => {
  it('should create an instance', () => {
    expect(new SessionTimerModel()).toBeTruthy();
  });
});
